name: train_random_forest
entry_points:
  main:
    parameters:
      trainval_artifact: {type: str, default: "trainval_data.csv:latest"}
      output_artifact: {type: str, default: "random_forest_export"}
    command: "python run.py"